function removeLastChildEl(parentEl) {
  //   console.log('entered', parentEl);
  //   console.log(parentEl.children.length);
  //   console.log(parentEl.lastElementChild);
  parentEl.removeChild(parentEl.lastElementChild);
}
